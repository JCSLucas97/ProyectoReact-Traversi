import { createTheme } from "@mui/material";

export const customPalette = createTheme({
  palette: {
    primary: {
      main: "#9BABB8",
    },
    secondary: {
      main: "#EEE3CB",
    },
  },
});
